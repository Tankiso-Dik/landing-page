# Core secrets
PAYLOAD_SECRET=dev-test-secret
CRON_SECRET=test-cron
PREVIEW_SECRET=test-preview

# App URLs
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Database (point to a test DB)
POSTGRES_URL=postgres://postgres:postgres@localhost:5432/payload_test

# Blob storage (point to test bucket)
BLOB_READ_WRITE_TOKEN=test-blob-token

# Optional other vars used in your repo
NEXT_PUBLIC_VERCEL_ENV=development
